# Glyphs UI

A Solana NFT explorer and management interface for Metaplex Core assets.

## Development

- `pnpm dev` – start dev server
- `pnpm build` – production build
- `pnpm test` – run full test suite
